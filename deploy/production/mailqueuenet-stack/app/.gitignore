# Ignore all runtime/persistent content under app/ by default.
*

# Keep directories (so we can commit empty structure).
!*/

# Keep ignore rules and placeholders.
!.gitignore
!**/.gitkeep
!**/.env.example

# Keep helper scripts.
!scripts/
!scripts/**

# Script configuration (contains credentials/secrets).
scripts/.env
!scripts/.env.example
